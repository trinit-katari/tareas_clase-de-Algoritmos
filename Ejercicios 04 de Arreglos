#Hacer un programa que dado 1 arreglo con los números del 1 al 10 genere las tablas demultiplicar y las imprima como tabla con sus resultados.
# Crear un arreglo con los números del 1 al 10
numeros = list(range(1, 11))

# Generar e imprimir las tablas de multiplicar
for numero in numeros:
    print(f"\nTabla del {numero}:")
    for i in range(1, 11):
        resultado = numero * i
        print(f"{numero} x {i} = {resultado}")
