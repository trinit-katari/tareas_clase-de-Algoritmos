#Hacer un programa en C que lea n números, los guarde en un arreglo busque los elementos pares y los cambie por unos (1), luego imprima el arreglo modificado
n = int(input("¿Cuántos números desea ingresar? "))
A = []

for i in range(n):
    num = int(input(f"Número {i+1}: "))
    A.append(1 if num % 2 == 0 else num)

print("Arreglo modificado:", A)
