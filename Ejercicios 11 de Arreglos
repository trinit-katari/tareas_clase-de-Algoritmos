#Hacer un programa en C que lea n números, y que los guarde en un arreglo, sólo si los elementos son múltiplos de 3. Imprimir el arreglo final
n = int(input("¿Cuántos números desea ingresar? "))
resultado = []

for i in range(n):
    num = int(input(f"Número {i+1}: "))
    if num % 3 == 0:
        resultado.append(num)

print("Múltiplos de 3:", resultado)
