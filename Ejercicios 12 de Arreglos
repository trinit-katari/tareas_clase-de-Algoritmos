#Hacer un programa en C que lea n números y los guarde en un arreglo A, ordenar los números de menor a mayor y guardarlos ordenados en un arreglo B, imprimir ambos arreglos
n = int(input("¿Cuántos números desea ingresar? "))
A = []

for i in range(n):
    A.append(int(input(f"Número {i+1}: ")))

B = sorted(A)

print("A:", A)
print("B (ordenado):", B)
