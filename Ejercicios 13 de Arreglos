#Hacer un programa en C que lea n números y los guarde en un arreglo A, ordenar el arreglo de mayor a menor e imprimir (sólo se debe usar 1 arreglo)
n = int(input("¿Cuántos números desea ingresar? "))
A = []

for i in range(n):
    A.append(int(input(f"Número {i+1}: ")))

A.sort(reverse=True)
print("Arreglo ordenado de mayor a menor:", A)
